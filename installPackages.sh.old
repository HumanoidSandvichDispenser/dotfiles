#/usr/bin/zsh
echo "${RESTORE}Installing ${GREEN}vim-plug${RESTORE}..."
mkdir -p ~/Downloads/
cd ~/Downloads/
sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
sh install.sh
